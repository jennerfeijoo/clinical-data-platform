[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "clinical-data-platform"
version = "0.10.0"
description = "An auditable clinical data platform with explicit execution lifecycles, durable failures, terminology normalization, and immutable lineage."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "Jenner Feijoo" }
]
keywords = ["clinical-data", "data-engineering", "healthcare", "postgresql", "audit"]
dependencies = [
    "psycopg[binary]>=3.2,<4"
]

[project.urls]
Repository = "https://github.com/jennerfeijoo/clinical-data-platform"

[project.scripts]
clinical-data = "clinical_data_platform.cli:main"

[project.optional-dependencies]
dev = [
    "mypy>=1.11",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6"
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"clinical_data_platform.contracts" = ["manifest.toml", "*/v*.toml"]
"clinical_data_platform.migrations" = ["V*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = [
    "integration: tests that require a running PostgreSQL instance"
]

[tool.ruff]
line-length = 101
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["clinical_data_platform"]
